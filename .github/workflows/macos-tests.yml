# see https://www.linkedin.com/pulse/github-actions-flutter-app-moataz-nabil
# see https://github.com/ReactiveCircus/android-emulator-runner
# see https://github.com/felangel/bloc/blob/master/.github/workflows/flutter_bloc.yaml

name: macOS Tests

on:
  workflow_dispatch:
  push: # macos should always be triggered to verify overall functionality, it is the fastest because it requires no simulators

env:
  flutter_version: '2.5.3'

jobs:
  test:
    name: Run tests (macOS)
    runs-on: macos-latest
    defaults:
      run:
        working-directory: nsd/example

    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Cache Flutter dependencies
        uses: actions/cache@v2
        with:
          path: /Users/runner/hostedtoolcache/flutter
          key: ${{ runner.OS }}-flutter-install-cache-${{ env.flutter_version }}

      - name: Set up Flutter environment
        uses: subosito/flutter-action@v1
        with:
          channel: 'stable' # 'dev', 'alpha', 'stable'
          flutter-version: ${{ env.flutter_version }}

      - name: Enable Flutter for macOS
        run: flutter config --enable-macos-desktop

      - name: Show Flutter configuration
        run: flutter doctor -v

      - name: Get Flutter dependencies
        run: flutter pub get

      - name: Run tests
        run: flutter test --no-pub --device-id macos --reporter expanded integration_test/nsd_test.dart
