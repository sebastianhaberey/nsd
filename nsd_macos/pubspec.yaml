name: nsd_macos
description: Flutter nsd plugin (macOS).
version: 1.0.0
homepage: https://github.com/sebastianhaberey/nsd

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      macos:
        pluginClass: NsdMacosPlugin
